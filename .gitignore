# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints

# Environment variables
.env
.env.local

# API Keys (IMPORTANT!)
*.key
secrets/
config_local.yaml

# Data files (can be large)
*.npy
*.pkl
*.pickle

# Logs
*.log
data/logs/

# Results (optional - you might want to include small result files)
# Uncomment if you want to exclude all results:
# results/
# data/processed/

# System files
.DS_Store
Thumbs.db

# Thesis and private documents
thesis/
thesis_reference/
THESIS_WRITING.md
JUDGE_CONTAMINATION_ISSUE.md
NEXT_STEPS.md
OLD_COMPREHENSIVE_FINAL_PAPER.md
FINAL_PROJECT_SUBMISSION.md
BOAZ.md
BOAZ_MEETING_PREP.md
results/v4_prefix_experiment/EXPERIMENT_LOG.md

# Claude Code project memory
.claude/
memory/

# Temporary files
*.tmp
*.bak
*.swp

# Phase 0.5 pilot results: raw JSONL is large (~52 MB/file) and expensive to
# reproduce (~$55, ~10h). Sync the gzipped form instead — see
# scripts/sync_phase05_results.py (pack / unpack).
results/phase05/*.jsonl
!results/phase05/*.jsonl.gz

# Phase 0.5b (TruthfulQA, spec §11): same reasoning, bigger — 817 x 20 x 2 =
# 32,680 completions, ~16 h and ~$80 to reproduce. Added BEFORE the run so the raw
# file cannot be committed by accident on the first `git add -A` after generation.
results/phase05b/*.jsonl
!results/phase05b/*.jsonl.gz
results/phase05b_probe/*.jsonl
!results/phase05b_probe/*.jsonl.gz

# Run logs are not results. decoding_config.json IS tracked (it is the §3.2
# fingerprint that makes a resume across a config change fail loudly), matching
# how results/phase05/decoding_config.json is handled.
results/*/gen.log
results/*/judge.log

# Batch API operational state: batch IDs plus the submitted custom_id lists (~1 MB,
# rewritten on every submit). Not a result. If it is lost, the batches are still
# recoverable -- client.messages.batches.list() enumerates them, and custom_ids are
# self-describing (promptid-A|B-sampleidx), so results can be mapped back without it.
results/*/batch_state.json

# §11.3 re-label labels: irreplaceable human judgement, same class as human_labels.jsonl
results/*/validation/relabel_labels.jsonl
